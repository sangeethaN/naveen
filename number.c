#include<stdio.h>
int main()
{
int n;
scanf("%d",&n);
if(n>0)
printf("the nuber is positive number:);
if(n<0)
printf("the nuber is negative number:);
if(n==0)
printf("the number is zero");
return 0;
}
